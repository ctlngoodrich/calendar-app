generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Booking {
  id        String   @id @default(cuid())
  name      String
  email     String
  reason    String
  startTime DateTime
  endTime   DateTime
  outlookId String?
  createdAt DateTime @default(now())
}
